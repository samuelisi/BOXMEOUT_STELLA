'use client';

// Alias component for the requested WalletConnectButton name.
// The existing implementation is WalletButton.

import { WalletButton } from './WalletButton';

export function WalletConnectButton(): JSX.Element {
  return <WalletButton />;
}
